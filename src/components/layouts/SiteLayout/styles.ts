import { styled } from 'styled-components';

export const Container = styled.div`
    max-width: 1536px;
    min-height: 100vh;
    margin-left: auto;
    margin-right: auto;
`;

export const Main = styled.main``;
