import { Banner } from './components/Banner';
import { Container, Title } from './styles';

export const Home = () => {
    return (
        <Container>
            <section>
                <Banner />
            </section>
        </Container>
    );
};
